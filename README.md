# chi-2-analyzer
Script to analyze oscilloscope data to calculate the chi-2 of a periodically poled silica fibre

## Installation & Dependencies

Ensure your execution environment has Python 3.9+ and the necessary scientific computing libraries installed:

```bash
pip install numpy scipy matplotlib
